version: "3.9"

services:
  cantina:
    image: ijimiguel/cantina:latest
    container_name: cantina
    ports:
      - "8080:80"  # maps Nginx port 80 to localhost:8080
    volumes:
      - ./config.json:/usr/share/nginx/html/config.json:ro
